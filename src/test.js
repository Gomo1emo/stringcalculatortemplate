
    if(numbers = "1,2"){
        var sum;
    var numArray = numbers.split(',');
    sum = parseInt(numArray[0]) + parseInt(numArray[1]);
   
    //return numArray;
}
    console.log(numArray);
    console.log(sum)